#!/bin/bash

echo "this project uses faster-whisper-server via HTTP API"
echo "no local model download needed"
echo ""
echo "to start the whisper server:"
echo "  docker run -p 9000:8000 fedirz/faster-whisper-server:latest-cpu"
